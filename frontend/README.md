# Moview Frontend

Vite + Preact app shell for Moview.

## Commands

| Command           | Action                              |
| :---------------- | :---------------------------------- |
| `bun run dev`     | Start the Vite dev server           |
| `bun run build`   | Build the production app to `dist/` |
| `bun run preview` | Preview the production build        |
